#NAME
name: my_workflow_dotnetapp

#TRIGGER
on:
  push:
    branches:
      - main
    paths:
      - 'dotnetwebapp/**'
      - '.github/workflows/dotnet-app.yml'
  pull_request:
    branches:
      - main
  workflow_dispatch:

#JOBS
jobs:
  my_doetnet_job:
    runs-on: ubuntu-latest
    
    steps:
      - run: dotnet --list-sdks

      - name: Download the code
        uses: actions/checkout@v4
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '10.0.101'

      - run: dotnet --list-sdks

      - name: Build
        run: dotnet build
      
      - name: Test
        run: dotnet test